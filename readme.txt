
File Search:
use ctrlp plugin- :CtrlP

Plugins:
My vim instance is setup with Pathogen as the plugin manager.
Plugins are located: ~/.vim/bundle/<pluginname>/

Install a plugin:
$ cd ~/.vim/bundle
$ git clone https://github.com/somePackage
#go to vim and tell pathogen to pull in the new docs- :Helptags
